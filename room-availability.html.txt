<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Room Availability - Hotel Booking System</title>
    <link rel="stylesheet" href="staff-styles.css">
    <style>
        .availability-container {
            max-width: 1400px;
            margin: 30px auto;
            padding: 20px;
        }

        .date-selector {
            background: white;
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 30px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .date-selector label {
            font-weight: 600;
            color: #333;
        }

        .date-selector input {
            padding: 10px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 1em;
        }

        .room-type-section {
            background: white;
            border-radius: 12px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .room-type-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 2px solid #e0e0e0;
        }

        .room-type-header h3 {
            color: #667eea;
            font-size: 1.8em;
            margin: 0;
        }

        .room-info-badge {
            display: flex;
            gap: 20px;
        }

        .badge {
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 0.9em;
        }

        .badge-price {
            background: #e3f2fd;
            color: #1976d2;
        }

        .badge-total {
            background: #f3e5f5;
            color: #7b1fa2;
        }

        .room-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .room-slot {
            aspect-ratio: 1;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
        }

        .room-slot.available {
            background: #e8f5e9;
            border-color: #4caf50;
        }

        .room-slot.available:hover {
            background: #c8e6c9;
            transform: scale(1.05);
        }

        .room-slot.booked {
            background: #ffebee;
            border-color: #f44336;
            cursor: not-allowed;
        }

        .room-number {
            font-size: 1.5em;
            font-weight: bold;
            color: #333;
        }

        .room-status {
            font-size: 0.85em;
            margin-top: 5px;
            font-weight: 600;
        }

        .room-slot.available .room-status {
            color: #4caf50;
        }

        .room-slot.booked .room-status {
            color: #f44336;
        }

        .guest-name {
            font-size: 0.75em;
            color: #666;
            margin-top: 5px;
            text-align: center;
            padding: 0 5px;
        }

        .legend {
            display: flex;
            gap: 30px;
            justify-content: center;
            margin: 30px 0;
            padding: 20px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .legend-item {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .legend-box {
            width: 40px;
            height: 40px;
            border-radius: 8px;
            border: 2px solid;
        }

        .legend-box.available {
            background: #e8f5e9;
            border-color: #4caf50;
        }

        .legend-box.booked {
            background: #ffebee;
            border-color: #f44336;
        }

        .stats-summary {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-bottom: 30px;
        }

        .summary-card {
            background: white;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .summary-card h4 {
            color: #666;
            font-size: 0.9em;
            margin-bottom: 10px;
        }

        .summary-card .number {
            font-size: 2.5em;
            font-weight: bold;
            color: #667eea;
        }

        @media (max-width: 768px) {
            .room-grid {
                grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
            }

            .stats-summary {
                grid-template-columns: 1fr;
            }

            .date-selector {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="nav-brand">
                <h1>🏨 Room Availability</h1>
            </div>
            <ul class="nav-menu">
                <li><a href="staff-dashboard.html">← Back to Dashboard</a></li>
                <li><a href="#" onclick="logout()" class="logout-btn">Logout</a></li>
            </ul>
        </div>
    </nav>

    <div class="availability-container">
        <!-- Date Selector -->
        <div class="date-selector">
            <label for="dateSelect">Select Date:</label>
            <input type="date" id="dateSelect" onchange="loadAvailability()">
            <button class="submit-btn" style="width: auto; margin: 0;" onclick="loadAvailability()">Check Availability</button>
        </div>

        <!-- Summary Stats -->
        <div class="stats-summary">
            <div class="summary-card">
                <h4>Total Rooms</h4>
                <div class="number" id="totalRooms">0</div>
            </div>
            <div class="summary-card">
                <h4>Available</h4>
                <div class="number" style="color: #4caf50;" id="availableRooms">0</div>
            </div>
            <div class="summary-card">
                <h4>Booked</h4>
                <div class="number" style="color: #f44336;" id="bookedRooms">0</div>
            </div>
        </div>

        <!-- Legend -->
        <div class="legend">
            <div class="legend-item">
                <div class="legend-box available"></div>
                <span>Available</span>
            </div>
            <div class="legend-item">
                <div class="legend-box booked"></div>
                <span>Booked</span>
            </div>
        </div>

        <!-- Room Type Sections -->
        <div id="roomSections"></div>
    </div>

    <script>
        // Check authentication
        if (sessionStorage.get